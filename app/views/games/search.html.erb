<div class="row">
  <% @games.each do |game| %>
  <div class="col-sm6 col-md-3">
    <div class="thumbnail">
      <%= link_to (image_tag game.image.url(:medium), class: 'image'), game %>
    </div>
  </div>
  <% end %>
</div>
